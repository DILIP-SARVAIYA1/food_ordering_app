import React from "react";
import ReactDOM from "react-dom";

const AppLayout = () => <div className="app">this is a react</div>;

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<AppLayout />);
